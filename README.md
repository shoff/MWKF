# MWKF
Small site for Midwest Kendo Federation

# install yeoman globally so we can use it from the command line
npm install -g yo

# make sure bower is installed
npm install -g bower

# install grunt
npm install -g grunt-cli

# install angular generator
npm install -g generator-angular

# start the yeoman angular generator
yo angular
 	
 	No to SASS
 	Yes to Bootstrap

 	starter modules 
 	no to all

